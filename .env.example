# Copia este archivo a `.env` y ajusta los valores.
# ¡Nunca commitees `.env`!

# Conexión a la base de datos PostgreSQL + pgvector
DATABASE_URL=postgresql+asyncpg://root:fc100711@localhost:5432/appointment

# Primer superusuario (se crea automáticamente al arrancar la app)
FIRST_SUPERUSER_EMAIL=admin@medapi.com
FIRST_SUPERUSER_PASSWORD=cfc100711

# Seguridad / JWT
SECRET_KEY=genera_una_clave_secreta_aleatoria
ACCESS_TOKEN_EXPIRE_SECONDS=90000

# Ollama / RAG (opciones por defecto; descomenta para cambiar)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text
# OLLAMA_LLM_MODEL=llama3.2
